.logon 10.226.0.34/ic_ccespedes,lzm142chj

.SET RECORDMODE OFF;
.SET FORMAT OFF;

.SET TITLEDASHES OFF;
.SET SEPARATOR ",";

.SET WIDTH 65531;.EXPORT REPORT FILE= "u:\data\canceladas\SITPEDCMS.csv"
SELECT TRIM(CODREQ) AS CODREQ,
TRIM(INDORIGREQ) AS INDORIGREQ,
TRIM(TIPREQ) AS TIPREQ,
TRIM(CODSRV) AS CODSRV,
TRIM(CODCLI) AS CODCLI,
TRIM(FECREG) AS FECREG,
TRIM(FECLIQ) AS FECLIQ,
TRIM(FECEST) AS FECEST,
TRIM(FECASG) AS FECASG,
TRIM(FECPRG) AS FECPRG,
TRIM(CODEDO) AS CODEDO,
TRIM(CODSIT) AS CODSIT,
TRIM(CODMOTV) AS CODMOTV,
TRIM(CODUSR) AS CODUSR,
TRIM(CODOFEPROD) AS CODOFEPROD,
TRIM(PLAZOCNTR) AS PLAZOCNTR,
TRIM(DESTIPREQ) AS DESTIPREQ,
TRIM(CODGRPREQ) AS CODGRPREQ,
TRIM(DESGRPREQ) AS DESGRPREQ,
TRIM(COD_PETICION) AS COD_PETICION,
TRIM(NUMTELEFVOIP) AS NUMTELEFVOIP,
TRIM(TIPPAQMUL) AS TIPPAQMUL,
TRIM(DISTRITOCD) AS DISTRITOCD,
TRIM(CODNOD) AS CODNOD,
TRIM(NROPLANO) AS NROPLANO,
TRIM(CODCLASRV) AS CODCLASRV,
TRIM(DESCLASRV) AS DESCLASRV,
TRIM(CODCNLVTA) AS CODCNLVTA,
TRIM(CODGRPVTA) AS CODGRPVTA,
TRIM(CODVDD) AS CODVDD,
TRIM(CODMOTACT) AS CODMOTACT,
TRIM(CODUSRGEN) AS CODUSRGEN,
TRIM(CODCTR) AS CODCTR,
TRIM(DOCUMENTO) AS DOCUMENTO,
TRIM(TIPODOC) AS TIPODOC,
TRIM(RUC) AS RUC,
TRIM(SEGMENTO) AS SEGMENTO,
TRIM(TELCONTACTO) AS TELCONTACTO,
TRIM(DESTINOTV) AS DESTINOTV,
TRIM(DESTINOBLOQUE) AS DESTINOBLOQUE,
TRIM(DESTINOINTERNET) AS DESTINOINTERNET,
TRIM(DESTINOVOZIP) AS DESTINOVOZIP,
TRIM(ORIGENTV) AS ORIGENTV,
TRIM(ORIGENBLOQUE) AS ORIGENBLOQUE,
TRIM(ORIGENINTERNET) AS ORIGENINTERNET,
TRIM(ORIGENVOZIP) AS ORIGENVOZIP,
TRIM(DESTINOVELOCIDADINTERNET) AS DESTINOVELOCIDADINTERNET,
TRIM(ORIGENVELOCIDADINTERNET) AS ORIGENVELOCIDADINTERNET,
TRIM(DESTINODECOALQUILER) AS DESTINODECOALQUILER,
TRIM(DESTINODECOVENTA) AS DESTINODECOVENTA,
TRIM(DESTINODECOCOMODATO) AS DESTINODECOCOMODATO,
TRIM(DESTINODECOOTROS) AS DESTINODECOOTROS,
TRIM(ORIGENDECOALQUILER) AS ORIGENDECOALQUILER,
TRIM(ORIGENDECOVENTA) AS ORIGENDECOVENTA,
TRIM(ORIGENDECOCOMODATO) AS ORIGENDECOCOMODATO,
TRIM(ORIGENDECOOTROS) AS ORIGENDECOOTROS,
TRIM(ORIGENMODEM) AS ORIGENMODEM,
TRIM(DESTINOMODEM) AS DESTINOMODEM,
TRIM(DESTINOMACADDRESSDECOALQUILER) AS DESTINOMACADDRESSDECOALQUILER,
TRIM(DESTINOMACADDRESSDECOVENTA) AS DESTINOMACADDRESSDECOVENTA,
TRIM(DESTINOMACADDRESSDECOCOMODATO) AS DESTINOMACADDRESSDECOCOMODATO,
TRIM(DESTINOMACADDRESSDECOOTROS) AS DESTINOMACADDRESSDECOOTROS,
TRIM(ORIGENMACADDRESSDECOALQUILER) AS ORIGENMACADDRESSDECOALQUILER,
TRIM(ORIGENMACADDRESSDECOVENTA) AS ORIGENMACADDRESSDECOVENTA,
TRIM(ORIGENMACADDRESSDECOCOMODATO) AS ORIGENMACADDRESSDECOCOMODATO,
TRIM(ORIGENMACADDRESSDECOOTROS) AS ORIGENMACADDRESSDECOOTROS,
TRIM(ORIGENMACADDRESSMODEM) AS ORIGENMACADDRESSMODEM,
TRIM(DESTINMACADDRESSOMODEM) AS DESTINMACADDRESSOMODEM
from pe_prod_esl_view.vw_sitpedcms where CODEDO='C' and substring(FECREG,4,7) IN ('06/2019','07/2019');

.EXPORT RESET
.LOGOFF
.EXIT